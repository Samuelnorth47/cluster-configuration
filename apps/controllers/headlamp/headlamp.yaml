apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: headlamp
  namespace: flux-system
spec:
  releaseName: headlamp
  targetNamespace: headlamp-system
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    force: true
  interval: 30m
  driftDetection:
    mode: enabled
  chart:
    spec:
      chart: headlamp
      version: "0.39.0"
      sourceRef:
        kind: HelmRepository
        name: headlamp
        namespace: flux-system
  values:
    config:
      useAccessToken: false
      usePKCE: false
      inCluster: true
      oidc:
        secret:
          create: false
        externalSecret:
            enabled: true
            name: headlamp-oidc-secret
    # -- Mount Service Account token in pod
    automountServiceAccountToken: true

    serviceAccount:
      # -- Specifies whether a service account should be created
      create: true
      # -- Annotations to add to the service account
      annotations: {}
      # -- The name of the service account to use.(If not set and create is true, a name is generated using the fullname template)
      name: "headlamp"

    clusterRoleBinding:
      create: true
      clusterRoleName: cluster-admin
      # -- Annotations to add to the cluster role binding
      annotations: {}
      # -- base url path at which headlamp should run




    ingress:
      enabled: true
      ingressClassName: traefik
      labels:
        app.kubernetes.io/part-of: traefik
      hosts:
        - host: headlamp.int.northcloud.dev
          paths:
            - path: /
              type: ImplementationSpecific
      tls: 
        - hosts:
            - headlamp.int.northcloud.dev

    initContainers:
      - command:
          - /bin/sh
          - '-c'
          - >-
            mkdir -p /headlamp/plugins && cp -r /plugins/* /headlamp/plugins/ && chown
            -R 100:101 /headlamp
        image: ghcr.io/headlamp-k8s/headlamp-plugin-flux:latest
        imagePullPolicy: Always
        name: headlamp-plugins
        securityContext:
          privileged: false
          runAsGroup: 0
          runAsNonRoot: false
          runAsUser: 0
        volumeMounts:
          - mountPath: /headlamp/plugins
            name: headlamp-plugins
    volumeMounts:
      - mountPath: /headlamp/plugins
        name: headlamp-plugins
    volumes:
      - emptyDir: {}
        name: headlamp-plugins 
